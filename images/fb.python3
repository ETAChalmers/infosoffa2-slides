width = 1920
height = 1080


with open("/dev/fb0", "wb") as fb0:
    for y in range(height):
        out = b''
        for x in range(width):
            out += b'\xff\x00\x00\x00'

        fb0.write(out)
        fb0.flush()
